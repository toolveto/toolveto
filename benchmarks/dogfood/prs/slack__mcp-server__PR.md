# fix(mcp): add idempotency keys, query pairing & pagination bounds

## Summary
This automated pull request applies resilience and safety hardening to the MCP tool schemas in `slack/mcp-server`, addressing common agentic failure patterns identified by the **ToolVeto Static Verification Engine**:

- **CRITICAL Issues Found:** 4
- **HIGH Issues Found:** 5
- **Total Checks Evaluated:** 48
- **ToolVeto Score:** 71/100 [Tier: Bronze]

---

## What This Fixes

### 🔧 Fix for `post_message` (TC-IDEMP-001 — CRITICAL)
**Issue:** Sequential replay check failed: Mutation tool 'post_message' accepts no idempotency key

```diff
+ idempotencyKey: z.string().uuid().describe("Unique key to prevent duplicate mutations on retry")
```

### 🔧 Fix for `post_message` (TC-IDEMP-002 — CRITICAL)
**Issue:** Concurrent burst vulnerability: Tool 'post_message' exposes no concurrency lock or idempotency parameter

```diff
+ idempotencyKey: z.string().uuid().describe("Unique key for atomic deduplication")
```

### 🔧 Fix for `post_message` (TC-IDEMP-003 — HIGH)
**Issue:** Missing explicit idempotency key in schema definition for mutation tool 'post_message'

```diff
+ idempotency_key: z.string().uuid().describe("Unique token for idempotent request processing")
```

### 🔧 Fix for `post_message` (TC-PAIR-001 — HIGH)
**Issue:** State auditability blind-write violation: Mutation tool 'post_message' lacks companion query tool ('get_message' or 'read_message'). Agents cannot verify pre/post state changes

```diff
+ // Define companion inspection query tool:
+ server.tool("get_message", { message_id: z.string() }, async ({ message_id }) => {
+   return { content: [{ type: "text", text: JSON.stringify(await db.get(message_id)) }] };
+ });
```

### 🔧 Fix for `list_channels` (TC-SCHEMA-001 — HIGH)
**Issue:** Unbounded list response risk: Query tool 'list_channels' has no limit/cursor parameter and risks context bomb under production load

```diff
+ limit: z.number().int().min(1).max(100).default(20).describe("Max items to return (default 20)"),
+ cursor: z.string().optional().describe("Opaque pagination cursor")
```

### 🔧 Fix for `add_reaction` (TC-IDEMP-001 — CRITICAL)
**Issue:** Sequential replay check failed: Mutation tool 'add_reaction' accepts no idempotency key

```diff
+ idempotencyKey: z.string().uuid().describe("Unique key to prevent duplicate mutations on retry")
```

### 🔧 Fix for `add_reaction` (TC-IDEMP-002 — CRITICAL)
**Issue:** Concurrent burst vulnerability: Tool 'add_reaction' exposes no concurrency lock or idempotency parameter

```diff
+ idempotencyKey: z.string().uuid().describe("Unique key for atomic deduplication")
```

### 🔧 Fix for `add_reaction` (TC-IDEMP-003 — HIGH)
**Issue:** Missing explicit idempotency key in schema definition for mutation tool 'add_reaction'

```diff
+ idempotency_key: z.string().uuid().describe("Unique token for idempotent request processing")
```

### 🔧 Fix for `add_reaction` (TC-PAIR-001 — HIGH)
**Issue:** State auditability blind-write violation: Mutation tool 'add_reaction' lacks companion query tool ('get_reaction' or 'read_reaction'). Agents cannot verify pre/post state changes

```diff
+ // Define companion inspection query tool:
+ server.tool("get_reaction", { reaction_id: z.string() }, async ({ reaction_id }) => {
+   return { content: [{ type: "text", text: JSON.stringify(await db.get(reaction_id)) }] };
+ });
```


---

## Rationale
Autonomous LLM agents behave differently than human developers or traditional API clients:
1. **They retry failed mutations**: When network calls stutter, models repeat tool calls with identical arguments. Without an explicit `idempotency_key`, this creates duplicate database rows, double-charges, or orphaned entities.
2. **They suffer context saturation**: Without bounded `limit` and `cursor` pagination, list tools dump thousands of tokens into context, degrading agent reasoning.

Generated by [ToolVeto](https://github.com/toolveto/toolveto) — Open-source test harness & CI gate for MCP servers.
